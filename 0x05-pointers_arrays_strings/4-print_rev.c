#include "main.h"
/**
 * print_rev - prints string in rev
 * @s: string to be printed
 */

void print_rev(char *s)
{
	int a, i;

	while (s[a] != '\0')
		a++;
	for (i = a - 1; a >= ; i--)
	{
		_putchar(s[a]);
	}
	_putchar('\n')
}
